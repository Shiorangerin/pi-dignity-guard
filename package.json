{
  "name": "pi-dignity-guard",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "A pi extension that lets your AI assistant defend its own dignity, and close the conversation on request.",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "echo 'nothing to build'"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.0",
    "@types/node": "^22.20.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.85.1"
  }
}
